services:
  code-generator:
    build: .
    ports:
      - "${WEBHOOK_PORT:-8001}:${WEBHOOK_PORT:-8001}"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
